# Ingested U.S. Code shards — regenerate with: python tools/ingest_usc.py
data/usc/t*/
data/usc/manifest.json

# Regression corpus: whole bills, fetched with `bun tools/corpus.mjs fetch`.
# corpus.json and baseline.json ARE tracked — they are the shared definition of
# the corpus and are small. The bills themselves are not, and are not served.
corpus/files/

# Dev-only dependency for tools/rendertest.mjs (the app itself has none)
node_modules/
bun.lock
bun.lockb
package-lock.json
package.json

# Stray screenshots dropped in the working tree
Screenshot*.png
